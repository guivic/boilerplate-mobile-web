import { globalStyles } from '../custom/styles';

export const Home = () => {
  const styles = globalStyles();

  return (
    <div className={styles.container}>
      Hello world
		</div>
	);
};

export default Home;
